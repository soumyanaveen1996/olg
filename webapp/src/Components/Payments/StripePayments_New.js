import React from "react";
// import { loadStripe } from "@stripe/stripe-js";
// import { Elements } from "@stripe/react-stripe-js";
import StripeCheckoutFormNew from "./StripeCheckoutForm_New";
import Config from "../../Utils/Config";

const StripePaymentsNew = (props) => {
	return null;
	// const stripePromise = loadStripe(Config.paymentGatewayKey);
	//
	// return (
	// 	<div>
	// 		<Elements stripe={stripePromise}>
	// 			<StripeCheckoutFormNew {...props} />
	// 		</Elements>
	// 	</div>
	// );
};
export default StripePaymentsNew;
